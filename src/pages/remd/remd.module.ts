import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RemdPage } from './remd';

@NgModule({
  declarations: [
    RemdPage,
  ],
  imports: [
    IonicPageModule.forChild(RemdPage),
  ],
})
export class RemdPageModule {}
